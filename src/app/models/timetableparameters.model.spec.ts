import { Timetableparameters } from './timetableparameters.model';

describe('Timetableparameters', () => {
  it('should create an instance', () => {
    expect(new Timetableparameters()).toBeTruthy();
  });
});
